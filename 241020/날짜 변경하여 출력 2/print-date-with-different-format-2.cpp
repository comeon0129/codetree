#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int month, day, year;
    cin >> month;
    cin.get();
    cin >> day;
    cin.get();
    cin >> year;
    cout<<year<<"."<<month<<"."<<day;
    return 0;
}